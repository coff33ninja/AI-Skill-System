# Gemini API Keys (add multiple for load balancing/rotation)
GEMINI_KEY_1=your_api_key_here
GEMINI_KEY_2=optional_second_key
GEMINI_KEY_3=optional_third_key

# Machine Identity
MACHINE_ID=my-machine
MACHINE_NAME=My Computer

# Mode: "voice" (hands-free), "text" (keyboard only), "hybrid" (both)
# voice  = Just speak, no typing needed. Best for accessibility/RSI/arthritis.
# text   = Traditional keyboard input only.
# hybrid = Both voice and keyboard work together.
MODE=voice

# Security
CONTROL_TIMEOUT_MS=300000
REQUIRE_CONFIRMATION=true

# Mesh Node (for remote control from other machines)
ENABLE_MESH=false
MESH_PORT=8080
